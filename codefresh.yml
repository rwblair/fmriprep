version: '1.0'
steps:
  build_image:
    type: build
    description: Building the image ...
    image-name: poldracklab/fmriprep
    tag: develop # ${{CF_BRANCH}}

  basic_test:
    image: poldracklab/fmriprep
    description: Cehck if --version runs ...
    commands:
      - fmriprep --version